import java.awt.Color;
import java.util.ArrayList;
import java.util.List;

public class IceCaster extends Tour {

    public IceCaster(String nom, int atk, int maxPv, double atkSpeed, int range, Element element, Position position, int cost, Color color) {
        super(nom, atk, maxPv, atkSpeed, range, element, position, cost, color);
    }
}